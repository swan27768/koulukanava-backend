services:
  db:
    image: postgres:16
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: koulukanava
    volumes:
      - koulukanava_pg:/var/lib/postgresql/data

volumes:
  koulukanava_pg:
